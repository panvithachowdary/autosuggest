# autosuggest
verify my git set-up :)

git ui:
1. add (plus button)
2. commit with message
3. push

git command:
1. git add -A
2. git commit -m"msg"
3. git push

git extra commands:
1.npm install -g nodemon
2.nodemon server.js 